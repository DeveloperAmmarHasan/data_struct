#include "bits/stdc++.h"
using namespace std;
//write a funciton to perform linear search
int search(int arr[],int size,int key){
    for(int i = 0;i< size ;i++){
        if (arr[i] == key){
            cout << arr[i] << endl;
        }
    }
}
